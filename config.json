{
  "celery": {
    "name": "caleydo_processing_queue",
    "host": "localhost",
    "port": 6379,
    "db": 5,
    "broker": "redis://${host}:${port}/${db}",
    "backend": "redis://${host}:${port}/${db}",
    "expires": 3600,
    "argv": "worker -l info --logfile=logs/%n%I.log"
  }
}
